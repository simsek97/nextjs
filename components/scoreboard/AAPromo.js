import React from 'react';

const AAPromo = ({ allAccessUrl }) => {
    return (
        <a href={allAccessUrl} className="all-access-link" target="_blank">
            <div className="all-access scoreboard">
            </div>
        </a>
    );
};

export default AAPromo;
